#!/usr/bin/env node

/**
 * Minimal placeholder for the future `create-my-design-system` CLI.
 * Intentionally lightweight so consumers get feedback immediately.
 */
console.log("🚀 Welcome to Create Design System Starter!");
console.log("Coming soon: this script will scaffold the monorepo into your target directory.");
console.log("For now, clone the repository manually and run `pnpm install && pnpm dev`.");
